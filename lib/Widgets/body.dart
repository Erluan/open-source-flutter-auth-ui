import 'package:flutter/material.dart';
import 'package:flutter_auth_ui/Widgets/background.dart';

class Body extends StatelessWidget {
  const Body({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    
    return Background(
      child: Column(
        children: [

        ],
      ),
    );
  }
}